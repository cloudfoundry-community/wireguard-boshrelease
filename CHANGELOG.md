# Changelog

## Unreleased

- Initial release: `wireguard` job (kernel interface via bpm-managed
  agent, `wg syncconf` reconcile, forwarding sysctls, hook arrays) and
  colocated `smoke-tests` errand. wireguard-tools 1.0.20260223 built
  from source. ubuntu-noble only.
